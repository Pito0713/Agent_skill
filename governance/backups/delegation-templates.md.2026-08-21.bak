# 派工 Prompt 模板（E）

> 五種常見任務型態的委派模板。`<>` 是填空，填完才准派工。
> 每份模板都內建派工三件套（目標與動機 / 驗收條件 / 回報格式）——刪掉任何一段就不符合 `model-orchestration.md` 第 3 節。
> **工具列的參數是 Claude Code 語法**；在 Codex / agy 上執行時查 `model-orchestration.md` §2 換成對應欄參數。
> **沒有 subagent 機制（或想省主 session 額度）時**：開一個獨立低成本 session（model 照 §5 選該 harness 的機械工/工作馬），把模板整包貼入當開場 prompt——模板內容本身 harness 中立，直接可用。

---

## T1. 搜尋 / 定位（找檔案、找定義、找引用）

**工具**：唯讀搜尋型 subagent。Claude Code：`subagent_type: "Explore"`（model 不必指定）；Codex：worker + prompt 內明寫「只讀不寫」；agy：research 型（參數名未驗證，以 schema 為準）

```
在這個 repo 找出：<要找什麼，例：所有呼叫 processPayment 的位置>
動機：<為什麼要找，例：準備修改它的錯誤處理，需要知道影響面>
搜尋廣度：<quick（單點查找）/ medium / very thorough（跨命名慣例全面掃）>

回報格式：
- 每筆結果一行：檔案:行號 + 一句話說明該處在做什麼
- 找不到就回「NOT FOUND」+ 你搜過哪些 pattern
- 不要貼大段程式碼，不要建議修改方式
```

**網路搜尋**改用 codex 模式 A（`skills/engineering/cli-delegate/SKILL.md`），不用 subagent。

---

## T2. 實作（新功能 / 修 bug 的執行）

**工具**：全工具 subagent + 隔離，model 選工作馬（§5）。Claude Code：`subagent_type: "general-purpose"` + `model: "sonnet"` + `isolation: "worktree"`；Codex：worker（雲端 sandbox 天然隔離，`gpt-5.4`）；agy：self + 隔離 workspace（未驗證）

```
## 目標
<一句話：做什麼>
動機：<為什麼做，上游需求是什麼>

## 範圍
允許修改：<檔案 / 目錄清單>
禁區（即使看起來相關也不能碰）：<檔案 / 目錄清單>

## 驗收條件（全部達成才算完成，缺一回報「進行中」）
- [ ] <可客觀驗證的條件，例：pytest tests/billing/ 全過，貼輸出>
- [ ] <例：新增 endpoint 用 curl 打通，貼 response>

## 不允許
- 不重構範圍外程式碼、不放寬既有測試、發現新問題記下來但不處理

## 回報格式
1. plan（你怎麼做的，≤5 行）
2. git diff --stat 輸出
3. 每條驗收條件的證據（測試輸出實貼）
4. 剩餘風險（無也要寫「無」）
```

---

## T3. 重構（行為不變的結構調整）

**工具**：同 T2；機械性 pattern 套用可降機械工（Claude `haiku` / Codex `gpt-5.4-mini` / agy `Gemini 3.5 Flash`）

```
## 目標
重構 <對象>：<改成什麼樣，例：把 X 模組的 callback 改為 async/await>
動機：<為什麼值得動它>

## 行為不變鐵律
- 重構前先跑一次測試記錄基準（貼輸出）
- 重構後測試結果必須與基準完全一致
- 禁止「順便」修 bug 或加功能——發現 bug 記下來回報，不修

## 範圍
允許修改：<清單>；禁區：<清單>

## 驗收條件
- [ ] 測試結果與基準一致（前後輸出都貼）
- [ ] diff 不含任何行為變更（無新增 if 分支、無修改條件式邏輯）

## 回報格式
基準測試輸出 / 重構後測試輸出 / diff --stat / 過程中發現但沒修的問題清單
```

---

## T4. 研究（理解 codebase / 技術方案調查）

**工具**：全工具 subagent（唯讀任務），model 選工作馬、跨模組複雜推理升複雜推理級（§5 各 harness 欄）。Claude Code：`subagent_type: "general-purpose"` + `model: "sonnet"`（升 `"opus"`）

```
## 問題
<要回答的具體問題，例：目前的 session 管理如何運作？token 存哪、多久過期、誰負責刷新？>
動機：<這個答案要拿來做什麼決策>

## 邊界
- 只讀不寫
- 讀檔範圍：<目錄提示，或「全 repo」>
- 回答不了的部分明說「未找到」，禁止推測補完

## 回報格式
- 結論先行（≤5 行）
- 每個論點附 檔案:行號 佐證
- 長分析寫到 <指定路徑，例 scratchpad>，回傳路徑
- 「確定的事實」與「推測」分開標示
```

---

## T5. 審查（對抗式 review）

**首選**：與 executor **不同模型家族**的 reviewer（盲點不重疊，見 model-orchestration §8）——Claude 寫的 code 用 codex 模式 C（照 `cli-delegate.md` 指令模板）；codex / agy 寫的可用 Claude subagent 審。
**異質來源不可用時**：本 harness 冷啟動 subagent（Claude Code：`subagent_type: "general-purpose"` + `model: "sonnet"`），不給任何主對話的分析脈絡：

```
你是獨立 code reviewer，沒看過其他人的意見，只根據以下原始碼回報問題。

審查維度：邏輯漏洞、邊界條件缺失、安全風險<、其他指定維度>
每個問題格式：[嚴重度] 檔案:行號：描述 → 潛在影響
嚴重度：CRITICAL / HIGH / MEDIUM / LOW
只回報問題，不提供修改方案。無問題輸出「未發現問題」。繁體中文。

---
<貼入 diff 或檔案內容>
```

**收到審查結果後**：主對話逐條查證（CONFIRMED / REJECTED / ESCALATE），流程照 `tech-lead-mode.md` Phase 4。**禁止**把 reviewer 輸出直接轉給 executor 修。

---

## 通用檢查（派工前 10 秒自檢）

- [ ] 三件套齊全（目標與動機 / 驗收條件 / 回報格式）
- [ ] 驗收條件可客觀驗證（有「貼輸出」「grep 命中」這類字眼）
- [ ] 改 code 的委派有加 `isolation: "worktree"`
- [ ] model 選擇對照過 `model-orchestration.md` 第 5 節
- [ ] 範圍與禁區寫死（實作 / 重構類）
