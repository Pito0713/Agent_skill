# Mentor 共用協定（正本）

> **這份檔案在執行時不載入。** 五份 `mentor-*/SKILL.md` 各自內聯了本協定的壓縮摘要，
> 執行時照那份摘要做即可，不需要讀這裡。
>
> 本檔的用途有兩個：
> 1. **正本**——摘要寫得不夠清楚、或要確認某個格式的完整長相時，人或模型來這裡查。
> 2. **模板**——新增第六個 mentor 時，照 §8 把變數填進去，避免又從零抄一份。
>
> 非 skill package（無 frontmatter、無 SKILL.md），不進 `skills/index.json`，
> 不進任何 harness 的路由表，不被 link farm 掛載。
>
> 建立於 2026-08-18，見 `memory/project-context.md` ADR-022。

---

## 為什麼是「內聯摘要」而不是「執行時讀這份檔」

抽共用段落有兩種做法，token 帳完全不同：

| 做法 | 單次呼叫成本 | 說明 |
|------|------------|------|
| 引用式（SKILL.md 指向本檔，執行時必讀） | 幾乎不省 | 省下的 SKILL.md 體積，等額付回在讀共用檔上；要同 session 第二次 mentor 呼叫才開始賺 |
| **內聯壓縮（現行）** | **每份省 123–228 tok** | 共用段壓成 12 行摘要留在 SKILL.md，共用檔不進執行路徑 |

選內聯的關鍵理由：**被抽掉的段落絕大多數是「用一行描述就能正確執行的格式」**
（末行輸出長什麼樣、迷思怎麼開場、知識圖譜四個箭頭），不是需要逐字複製的規格。
真正需要逐字精確的只有 Phase 0 那行 vault 路徑，摘要裡直接寫死完整路徑即可。

**節省幅度要誠實記錄**（2026-08-18 實測，`bytes/3.5` 口徑）：

```
neuro   2902→2678 (-224)   tech    3352→3167 (-185)   science 3650→3422 (-228)
society 3504→3295 (-209)   invest  3719→3596 (-123)   合計 17127→16158 (-969)
```

規劃時預估每份省 350–400，實際只有 123–228。**差距的原因是摘要本身要價約 300 tok**——
要保住五個 domain 各自不同的關係詞、箭頭標籤、迷思符號、文獻來源，摘要就壓不下去。
被移除的共用段約 450–500，扣掉摘要淨值就只剩兩百上下。

因此**本次重構的主要價值不是省 token，是消除五份重複與建立新增 mentor 的模板**。
若日後要真正大幅瘦身，槓桿更大的是各 SKILL.md 裡的兩段純冗餘（見 §10），不是這裡。

**代價要說清楚**：五份摘要之間可能各自漂移，沒有任何機制會偵測。
改動協定時必須手動同步五份 + 本檔，見 §9。

---

## §1 Phase 0：讀取 Vault 上下文

原始完整版（各 mentor 曾各存一份）：

```bash
source "$HOME/Agent_skill/.env" 2>/dev/null
<DOMAIN_UPPER>_DIR="${OBSIDIAN_VAULT_PATH}/knowledge/<domain>"

EXISTING_NOTES=$(ls "$<DOMAIN_UPPER>_DIR"/*.md 2>/dev/null | xargs -I{} basename {} .md)

if [ -z "$EXISTING_NOTES" ]; then
  echo "📂 knowledge/<domain>/ 目前為空，從頭建立知識節點"
else
  echo "📂 使用者已有筆記："
  echo "$EXISTING_NOTES"
  echo "→ 確認：這些概念是否與當前問題相關？優先延伸，不重複。"
fi
```

**壓縮後等價於**：`ls "$OBSIDIAN_VAULT_PATH/knowledge/<domain>"/*.md`，有結果就進 §2，沒有就跳過 §2。

**上下文注入規則（共用四項）：**

```
[ ] 讀出現有筆記標題與標籤
[ ] 確認使用者已掌握哪些相關概念（已掌握 → 直接跳到進階層，不重講基礎）
[ ] 回覆中主動標注「你在 [[note-name]] 已記錄了 X，本次延伸 Y」
[ ] 當前問題與多個既有筆記交叉 → 優先說明節點間的連結關係
```

各 domain 額外一項，見 §8「Phase 0 附加規則」欄。

---

## §2 【0. 你已有的節點】區塊

```
你在 [[note-name]] 記錄了「<既有概念>」。
本次的 <新主題> 是它的 <關係詞>。
```

`<關係詞>` 各 domain 不同，見 §8。vault 無相關筆記時整個區塊跳過。

> mentor-science 額外分支：若當前問題需要先備知識而使用者未建立，補一句
> 「這個概念需要先理解 <先備概念>，我先簡短說明，再展開主題。」

---

## §3 確定性四級（符號與等級名共用）

| 符號 | 等級 |
|------|------|
| ✅ | 高度確立 |
| ⚠️ | 合理推論 / 情境成立 |
| ❓ | 初步證據 / 待驗證 |
| 🚫 | 該領域的迷思 |

**第三欄「本領域的認定標準」是各 mentor 的核心差異，必須留在各自 SKILL.md，不得抽到這裡。**
各領域的鐵律（三條或四條）同理，不抽。

---

## §4 迷思雷達的處理格式

迷思**表格本身**屬各 domain，留在各自 SKILL.md。共用的只有命中之後的處理格式：

```
<符號> 你的問題包含一個常見的<領域>迷思：「<迷思內容>」
正確說明：<領域層面的解釋，含確定性符號>
這個迷思流傳的原因：<原因類型>
確認後，讓我繼續回答你真正的問題：<繼續展開>
```

`<符號>` 與 `<原因類型>` 見 §8。**時機一律是「回覆開頭優先指出，再展開正文」**，不是等使用者問。

---

## §5 知識圖譜節點區塊

```
【這個概念的位置】
<箭頭標籤組>

建議存入：knowledge/<domain>/<YYYY-MM-DD>-<主題>.md
```

`<箭頭標籤組>` 各 domain 不同（見 §8），但四個位置的語意固定：

| 箭頭 | 語意 |
|------|------|
| ↑ | 更基礎 / 先備 / 底層依賴 |
| ↔ | 同層並列 / 交叉 / 替代方案；已有筆記寫 `[[筆記名]]`，沒有寫 `[建議下一個節點]` |
| ↓ | 延伸 / 推導 / 上層應用 |
| × | 跨領域連結（指向其他 `knowledge/<domain>/`） |

---

## §6 末行固定輸出

vault 有既有筆記：

```
💾 說「記起來」可將此回答存入 knowledge/<domain>/，與你的「<最相關既有筆記>」形成節點連結。
```

vault 無既有筆記：

```
💾 說「記起來」可將此回答存入 knowledge/<domain>/ 作為<首節點descriptor>。
```

---

## §7 共用 Checklist 項

五份 Checklist 中逐字相同的部分。各 SKILL.md 的 Checklist 只留 domain 專屬項，
共用項由此處統一定義：

```
[ ] Phase 0 已執行：讀取 knowledge/<domain>/ 並確認既有筆記
[ ] 若有相關既有筆記，已在 【0. 你已有的節點】 標注
[ ] <領域>迷思雷達已掃描問題前提，發現迷思優先處理
[ ] 文獻引用真實存在，或已明確說明「建議查閱 <文獻來源>」——絕不捏造文獻
[ ] 知識圖譜節點已說明上下游概念，跨領域交叉已標注
[ ] 末行已加上建議存檔提示
```

---

## §8 各 domain 變數對照表

| 變數 | neuro | society | science | tech | invest |
|------|-------|---------|---------|------|--------|
| `<domain>` | `neuro` | `society` | `science` | `tech` | `invest` |
| `<領域>` | 神經科學 | 社會 | 科學 | 工程 | 投資 |
| `<文獻來源>` | PubMed | Google Scholar | 教科書章節 | 官方規格 / RFC | Google Scholar |
| `<首節點descriptor>` | 第一個神經科學節點 | 第一個社會科學節點 | 第一個自然科學節點 | 第一個技術架構節點 | 第一個投資框架節點 |
| §2 `<關係詞>` | 上游 / 下游 / 交叉路徑 | 交叉 / 延伸 / 對比 | 上層基礎 / 推導結果 / 跨科應用 | 上層架構 / 底層實作 / 替代方案 / 應用場景 | 相同偏誤的不同表現 / 延伸框架 / 對應策略 |
| §4 `<符號>` | ⚠️ | ⚠️ | 🚫 | 🚫 | 🚫 |
| §4 `<原因類型>` | 為何如此廣泛流傳 | 媒體簡化 / 確認偏誤 / 研究被誤讀 | 語言誤用 / 媒體簡化 / 直覺與現實不符 | 廠商宣傳 / 成功案例偏差 / 過度簡化 | 媒體敘事 / 生存者偏差 / 確認偏誤 |
| §5 箭頭標籤 | ↑上游概念 ↔交叉節點 ↓下游概念 | ↑上游概念 ↔交叉節點（含 neuro 交叉）↓下游延伸 | ↑基礎概念（先備）↔同層概念 ↓推導概念 ×跨科連結 | ↑底層依賴 ↔同層替代方案 ↓上層應用 ×跨領域 | ↑基礎概念 ↔相關框架 ↓延伸應用 ×跨域（neuro／society）|
| §1 Phase 0 附加規則 | 已知核心機制 → 跳進階層 | 跨 society+neuro 主動連結兩資料夾 | 先備概念未建立 → 先補先備知識；跨 science+neuro 連兩資料夾 | 跨 tech+science（如密碼學數學原理）→ 連結 knowledge/science/ | （無）|

---

## §9 維護規約

**改協定時的同步清單**（沒有任何自動檢查會擋你，這是純自律規約）：

```
[ ] 改本檔對應章節
[ ] 五份 mentor-*/SKILL.md 的「標準 mentor 協定」摘要區塊逐一同步
[ ] 改動涉及行為 → memory/project-context.md 追加 ADR
[ ] bash bin/token-budget.sh --strict
[ ] python3 bin/validate-skill-index.py
```

**新增第六個 mentor 時**：

1. 在 §8 表格加一欄，填滿七個變數
2. 照既有 mentor 的結構寫 SKILL.md：`標準 mentor 協定摘要`（從 §8 展開）→ 角色定位 →
   思考路徑 → 領域確定性標準表 + 鐵律 → 回覆結構主體 → 迷思雷達表 → 觸發條件 → domain Checklist
3. **不要**把本檔的完整內容抄進新 SKILL.md——那是這次重構要消除的東西
4. 走 `skills/convert-skill/SKILL.md` 流程更新 `skills/index.json` 與 `skills/llms.txt`

**明確不屬於本協定、不得往這裡搬的東西**：角色定位、思考路徑、各領域確定性標準、
鐵律、知識類型分類表、回覆結構的主體區塊、迷思雷達表格內容、觸發條件。
這些是各 mentor 的存在理由，抽掉就沒有專科 mentor 了。

---

## §10 已識別但本次未處理的冗餘（2026-08-18 使用者裁決「先不動」）

記在這裡是為了不讓分析結果隨對話消失。**這兩段的省下幅度比 §1–§7 的骨架抽取更大，
而且是無條件節省（不需要摘要換回去）**，日後要再瘦身應該先從這裡下手：

| 冗餘段 | 每份約 | 為什麼是冗餘 |
|--------|------:|------------|
| 「觸發條件 → 直接觸發」關鍵詞列表 | ~120 tok | 這些詞已在 frontmatter `description` 裡，而 description 是**常駐在 system prompt** 的。SKILL.md 內這份要等 skill 載入後才被讀到——那時路由早已決定完畢，對任何決策零影響。同段的「需主題確認」分流條款有價值，應保留 |
| 「與其他 mentor 的根本差異」比較表 | ~200 tok | 五張表比較對象各不相同（science 比 neuro/society、tech 只比 science、invest 比一個不存在的 mentor-business），彼此不一致；且下方「觸發條件」段的路由表又講一次同樣的分工 |

若要處理，建議做法：刪掉直接觸發詞列表；五張差異表合併成一張完整的 5×N 路由矩陣放本檔，
各 SKILL.md 只留一句自身定位。預估合計可再省約 1,600 tok（5 份 × 320）。
