# Handoff Document

**時間**：2026-07-06
**任務**：審查 setup.sh / inject.sh 的 governance 分發路徑調整 → 修復 setup.sh 斷鏈缺陷（v4.9）
**狀態**：🟢 完成（本輪目標全數達成，工作區乾淨，唯一待辦是 push）

---

## 背景

延續 v4.7 governance 分發。另一 session 把 governance 下游路徑從巢狀的 `~/.claude/skills/governance/...` 改為一級的 `~/.claude/governance/...`，並在 setup.sh 直建第二條 symlink（未 commit）。本輪 session 對這批 sh 變更做功能覆蓋審查，發現並修復一個真實斷鏈缺陷後一起 commit 成 v4.9。

## 當前進度

### 已完成

- [x] **審查未 commit 的 sh 變更**：隔離假 HOME 實跑 5 組測試，確認 inject.sh 情境 1/2 正常、setup.sh 主流程正常
- [x] **發現並修復 setup.sh 斷鏈缺陷**：原順序「刪舊 skills symlink → 檢查 governance → 建新鏈」，若 governance 位置被實體目錄佔住會半途 exit，留下 skills 舊鏈已刪、新鏈未建的斷鏈中間態。修法：存在性檢查全部前置
- [x] **v4.9 commit**（`cd8d0bd`）：sh 路徑一級化 + 斷鏈修復 + ADR-007 修訂節 + README v4.9 + lessons 追加 + 補 commit handoff-20260704.md

### 進行中

- （無）

### 待處理

- [ ] `git push`：本機 main 領先 origin/main **1 個 commit**（`cd8d0bd`），使用者尚未指示 push
- [ ] **各機器需重跑 `bash ~/Agent_skill/setup.sh`** 才會建立 `~/.claude/governance` 一級 symlink（本機已於 2026-07-06 完成並實測可達；其他機器尚未）
- [ ] 既有下游專案重跑 `bash ~/Agent_skill/inject.sh` 取得新路由段（舊 `skills/governance` 路徑仍相容，不急）
- [ ] 下次開 Antigravity（agy）session 照 model-orchestration §2 適配表實跑委派，驗證 `TypeName` / `Workspace` 參數名（v4.8 遺留，本端無法驗證）
- [ ] `memory/project-context.md` 技術選型欄位仍是模板占位（長期）

---

## 重要決策

| 決策 | 選擇 | 原因 |
|------|------|------|
| governance 分發路徑 | 改一級 `~/.claude/governance`（setup.sh 直建），舊巢狀路徑保留相容 | 一級路徑語意清楚；代價是各機器重跑 setup.sh（ADR-007 修訂節） |
| setup.sh 檢查順序 | 存在性檢查全部前置，通過後才 rm/ln | 消除「舊鏈已刪、新鏈未建」的斷鏈中間態，與 v4.6 rules/ 斷鏈同類問題 |
| 是否採納原始 sh 變更 | 採納但補修復 | 路徑一級化本身合理，但引入了半途退出缺陷，一起修掉才 commit |

---

## 關鍵檔案

```
setup.sh                     # 建 ~/.claude/skills + ~/.claude/governance 兩條 symlink（檢查已前置）
inject.sh                    # INJECT_BLOCK 制度層路由指 ~/.claude/governance/...
skills/governance            # → ../governance（v4.7 舊路徑相容層，勿刪）
memory/project-context.md    # ADR-001~008，ADR-007 含 v4.9 修訂節
governance/lessons.md        # 5 條教訓（最新：多步驟腳本先驗後動）
memory/handoff-20260704.md   # 上一輪交接（v4.6~v4.8），附 v4.9 後記
```

---

## 已知問題 / 風險

- ⚠️ **跨機器不一致**：v4.9 的一級 symlink 只在本機建好，其他機器 pull 後 `~/.claude/governance` 不存在，inject 過的下游若已用新路徑會斷鏈 → 每台機器都要重跑 setup.sh
- ⚠️ **governance/backups/ 已 8 個備份**（門檻 10），下下輪 🟡 修改前該問使用者清理
- ⚠️ **Antigravity 參數未經本端驗證**（v4.8 遺留）：適配表 `TypeName`/`Workspace` 來自 agy session 回報

---

## 接手後第一步

1. `git status` + `git log origin/main..HEAD --oneline` 確認起點（預期：乾淨 + 領先 origin 1 commit，除非已 push 或本 handoff 已 commit）
2. 問使用者是否 push
3. 若切換機器：先 `bash ~/Agent_skill/setup.sh` 重建 symlink

---

## 有用的指令

```bash
# 驗證 governance 一級路徑可達（v4.9 核心）
ls ~/.claude/governance/model-orchestration.md ~/.claude/governance/judgment-rubrics.md

# symlink 鏈整體健檢
ls -l ~/.claude/ | grep -E "skills|governance"

# 下游注入 / 更新
bash ~/Agent_skill/inject.sh
```

---

## 相關連結

- ADR-007（含 v4.9 修訂）/ ADR-008：`memory/project-context.md`
- 制度修改權限：`governance/maintenance-protocol.md`（setup.sh / governance 檔 = 🟡 先問）
