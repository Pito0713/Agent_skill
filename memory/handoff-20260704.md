# Handoff Document

**時間**：2026-07-04 下午
**任務**：接手 Agent_skill 制度倉庫 → 收尾 v4.6 → 執行 v4.7 governance 分發、v4.8 harness 適配層
**狀態**：🟢 完成（本輪目標全數達成，工作區乾淨，剩 push 與跨環境驗證）

---

## 背景

本 repo 是 AI agent 制度倉庫（rules / skills / agents / governance / memory），經 setup.sh symlink 與 inject.sh 注入分發到下游專案。本輪 session 接手時工作區有整批未 commit 的 v4.6 全面 review 變更，之後依使用者指示逐步完成分發、健檢、跨 harness 適配。

## 當前進度

### 已完成

- [x] **v4.6 收尾**（`c857cdf`）：前一 session 的全面 review（rules/ 斷鏈修復 `skills/rules → ../rules`、code-review 冷啟動交叉驗證等 28 檔）
- [x] **v4.7 governance/ 分發到下游**（`1db3450`）：`skills/governance → ../governance` 相對 symlink + inject.sh「制度層路由」段（用到才讀，不 @ 常駐）；inject.sh 情境 1/2 實跑驗證通過；ADR-007
- [x] **hermes-extractor 處置**（`5656293`）：另一 session 新增的 skill 經評估（6/10，四個制度整合缺陷）後被刪除，llms.txt 懸空索引 5 行已移除
- [x] **harness 完整度測試**：47 項宣告路徑全數可達、工具鏈全過、制度健康門檻內（腳本在 session scratchpad，未入 repo）
- [x] **v4.8 harness 適配層**（`f680862`）：model-orchestration §2 改 Claude Code / Antigravity 雙欄參數適配表 + 派發後等待紀律；tech-lead-mode Phase 2 標註語法歸屬；coding-workflow-core Phase 0 原生工具優先、shell 降 fallback；ADR-008 + lessons 追加

### 進行中

- （無）

### 待處理

- [ ] `git push`：本機 main 領先 origin/main **4 個 commit**（c857cdf / 1db3450 / 5656293 / f680862），使用者尚未指示 push
- [ ] 既有下游專案重跑 `bash ~/Agent_skill/inject.sh` 取得 governance 路由段（symlink 部分已即時生效）
- [ ] 下次開 Antigravity（agy）session 時，照 model-orchestration §2 適配表實跑一次委派，驗證 `TypeName` / `Workspace` 參數名（目前僅有該 session 回報，Claude Code 端無法驗證）
- [ ] `memory/project-context.md` 技術選型欄位仍是模板占位（長期待辦）
- [ ] 本交接文件（memory/handoff-20260704.md）尚未 commit

---

## 重要決策

| 決策 | 選擇 | 原因 |
|------|------|------|
| governance/ 分發方式 | `skills/governance → ../governance` 相對 symlink，不改 setup.sh | 比照 ADR-006 rules/ 模式：零腳本改動、隨 clone 位置有效（ADR-007） |
| 下游 governance 載入方式 | 純文字路由指標（用到才讀），不 @ 常駐 | 四檔常駐會重演 v4.5 修掉的 901 行常駐問題 |
| Antigravity 相容性修法 | 建 harness 適配表，**拒絕**「整批替換成 agy 參數」的原始建議 | repo 未遷移，Claude Code 仍是主環境；單邊替換 = 反向撞牆（ADR-008） |
| hermes-extractor 懸空索引 | 移除（使用者選定），非重寫 | skill 本體已被刪、有四個制度整合缺陷未修 |
| v4.6/v4.7 混版檔案拆 commit | 暫存 v4.7 → 還原提交 v4.6 → 放回提交 v4.7 | 保持版本歷史語意乾淨，並經逐檔行數比對驗證無交叉污染 |

---

## 關鍵檔案

```
governance/model-orchestration.md      # §2 harness 適配表（本輪重寫核心）
skills/engineering/tech-lead-mode.md   # Phase 2 委派協定（雙 harness 語法）
skills/engineering/coding-workflow-core.md  # Phase 0 原生工具優先偵測
inject.sh                              # INJECT_BLOCK 含制度層路由段（單一事實來源）
skills/governance                      # → ../governance symlink（v4.7 新增）
memory/project-context.md              # ADR-001 ~ ADR-008 全記錄
governance/lessons.md                  # 4 條教訓（最新：harness 方言、路徑實測）
```

---

## 已知問題 / 風險

- ⚠️ **多 session 並行掃入風險**：本輪 `git add -u` 曾把另一 session 加進 llms.txt 的 5 行掃進 v4.6 commit（後因該 skill 被刪而變懸空）。commit 前先 `git status` 比對「自己動過的檔案清單」是必要動作
- ⚠️ **Antigravity 參數未經本端驗證**：適配表的 `TypeName: self/research`、`Workspace: branch/share` 來自 agy session 回報；若實跑報錯，照 maintenance-protocol 🟢 級事實修正更新表格
- ⚠️ **governance/backups/ 已 7 個備份**（門檻 10），再兩三輪 🟡 級修改就該問使用者清理

---

## 接手後第一步

1. `git status` + `git log --oneline -5` 確認起點（預期：乾淨 + 領先 origin 4 commits，若 handoff 文件已 commit 則 5）
2. 問使用者是否 push；若使用者在下游專案工作，提醒重跑 inject.sh

---

## 有用的指令

```bash
# harness 完整度全檢（路徑可達性 / symlink 鏈 / llms.txt 一致性 / 工具可用性）
# 腳本已隨 session scratchpad 清除，重建時參考 maintenance-protocol §5 清單，核心檢查：
ls ~/.claude/skills/rules/coding-standards.md ~/.claude/skills/governance/judgment-rubrics.md  # symlink 鏈
grep '^path:' skills/llms.txt | awk '{print $2}' | while read p; do [ -f "$p" ] || echo "斷鏈: $p"; done

# 下游注入 / 更新
bash ~/Agent_skill/inject.sh
```

---

## 相關連結

- ADR-007 / ADR-008：`memory/project-context.md`
- 制度修改權限：`governance/maintenance-protocol.md`（governance 檔 = 🟡 先問）

---

> **2026-07-06 後記（v4.9）**：本文提及的 governance 下游路徑 `~/.claude/skills/governance/...` 已改為一級路徑 `~/.claude/governance/...`（setup.sh 直建 symlink），舊路徑保留相容。詳見 ADR-007 修訂節。
