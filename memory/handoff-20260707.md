# Handoff Document

**時間**：2026-07-07
**任務**：v5.0 三 harness 制度統一（單一正本 + 三薄索引 + 全域接線 + 記憶收斂）
**狀態**：🟢 制度產出全數落檔；對抗審查與 commit 見下方待處理

---

## 背景

使用者委託（一次性強模型 session）：把判斷力轉成「一份共用正本 + 三個薄索引」制度，讓 Claude Code / Codex / Antigravity（agy）三 harness 的弱模型 session 共用且不漂移。盤點發現制度骨架（governance/ 七檔）已存在，但**只接線了 Claude**：`~/.codex/AGENTS.md` 與 `~/.gemini/GEMINI.md` 從未建立，Codex/agy 在非本 repo 專案裡零制度；記憶分裂三處無正本。

## 使用者定案（四題）

1. 正本 = 本 repo（不建 `~/.agents/institution/`）
2. agy 升級為完整 harness（解鎖 excludeTools，備份 `~/.gemini/settings.json.2026-07-07.bak`、`~/.agents/settings.json.2026-07-07.bak`）
3. 記憶收斂：交接正本 = `~/.agent-sessions/<專案>/latest.md`；教訓 = lessons.md；Codex Memories 保持關閉
4. 收尾只 commit 不 push；備份全留

## 本輪變更

| 檔案 | 變更 |
|------|------|
| `setup.sh` | 增建 `~/.codex/AGENTS.md`、`~/.gemini/GEMINI.md` 兩條全域 symlink（前置檢查同 v4.9 模式）；本機已跑、四鏈驗證可達 |
| `AGENTS.md` | 改寫：Codex 薄索引，絕對路徑，Codex 專屬區塊（gpt-5.5/5.4/5.4-mini、effort、32KiB、Memories 關閉） |
| `GEMINI.md` | 改寫：agy 完整 harness 薄索引 + 被委派模式（A/B/C 唯讀約定保留）+ agy 模型池（本機 `agy models` 實測） |
| `CLAUDE.md` | +2 行：三索引關係、鐵律 6 加 agent-sessions 交接 |
| `governance/model-orchestration.md` | §2 補 Codex 欄、§5 三 harness 型號表（查證日期 2026-07-07）、§8 跨 harness 分工（新）、§9 未確認清單 |
| `governance/harness-diagnosis.md` | 追加三 harness 差異診斷（各家獨有 TOP 問題 + 修法） |
| `governance/maintenance-protocol.md` | §6 記憶回寫正本表（新）、§7 索引防漂移四查（新，指令已實跑通過）、🟡 納入三索引 |
| `governance/delegation-templates.md` | 五模板工具列改三 harness 對照 + 低成本 session 交辦法 |
| `governance/letter-to-future-sessions.md` | 改寫：波及面、退化方式（索引漂移/記憶不回寫居首）、信心最低清單 |
| `governance/lessons.md` | append 2026-07-07 條（制度只接線一家的靜默失效） |
| `memory/project-context.md` | ADR-009 |
| `skills/engineering/tech-lead-mode.md` | Path B 改寫：agy 已解鎖可直接當 executor，防線移到工單層（審查發現的懸空引用） |
| `skills/engineering/gemini-assist.md` | 移除已停服的 gemini CLI fallback（審查發現與 model-orchestration §2 打架） |
| repo 外 | `~/.agent-sessions/Agent_skill/latest.md` 更新；Claude 專案記憶修至現況；兩份 settings 解鎖 |

## 待處理

- [x] 對抗審查（fresh-context opus，2 HIGH / 3 MEDIUM / 1 LOW）：HIGH×2 + MEDIUM×3 已修並復驗；LOW（setup.sh rm→ln 兩步）刻意不改——macOS `ln -sf` 對指向目錄的既有 symlink 需 `-h` 旗標，現行寫法較安全
- [ ] `git push`：領先 origin 的 commits（含 cd8d0bd）待使用者指示
- [ ] **其他機器重跑 `bash ~/Agent_skill/setup.sh`**（否則該機 Codex/agy 靜默無制度）
- [ ] 第一個 Codex session：校準 model-orchestration §5 Codex 欄（零本機實測）
- [ ] 第一個 agy 委派：驗證 `TypeName`/`Workspace` 參數名（v4.8 遺留）

## 接手後第一步

1. `git -C ~/Agent_skill status`（多 harness 並行是常態，非預期變更先問）
2. 讀 `governance/letter-to-future-sessions.md`（含信心最低清單）
