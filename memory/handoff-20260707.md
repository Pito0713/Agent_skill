# Handoff Document

**時間**：2026-07-07
**任務**：v5.0–v5.0.2 三 harness 制度統一 + 對抗審查 ×2 + 文件對齊
**狀態**：🟢 完成（本輪目標全數達成、雙重對抗審查修畢、工作區乾淨；唯一待辦是 push）

---

## 背景

使用者以一次性強模型 session 委託：把判斷力轉成「一份共用正本 + 三個薄索引」制度，讓 Claude Code / Codex / Antigravity（agy）三 harness 的弱模型 session 共用且不漂移。盤點發現制度骨架（governance/ 七檔）已存在，但**只接線了 Claude**——Codex/agy 的全域載入點從未建立，非本 repo 專案裡兩家零制度；記憶分裂三處無正本。本輪 = 補全域接線 + 修漂移 + 填 Codex 缺口 + 記憶收斂。

## 使用者定案（五題）

1. 正本 = 本 repo（不建 `~/.agents/institution/`）
2. agy 升級為完整 harness（excludeTools 解鎖，備份 `~/.gemini/settings.json.2026-07-07.bak`、`~/.agents/settings.json.2026-07-07.bak`）
3. 記憶收斂：交接正本 = `~/.agent-sessions/<專案>/latest.md`、教訓 = lessons.md；Codex Memories 保持關閉
4. 只 commit 不 push；備份全留
5. Claude 全域缺口 → 建極薄全域指標（CLAUDE.global.md，18 行不常駐載入）

## 本輪 commits（本機領先 origin/main 5 個，含上輪 cd8d0bd）

| commit | 內容 |
|--------|------|
| `b91b29c` v5.0 | 三 harness 統一：setup.sh 全域 symlink、AGENTS/GEMINI 薄索引改寫、model-orchestration Codex 欄 + 三家型號表（2026-07-07 查證）+ 跨 harness 分工、maintenance-protocol §6 記憶回寫 / §7 防漂移四查、harness-diagnosis 三家差異診斷、delegation-templates 三家對照、letter 改寫、ADR-009、opus 對抗審查 5 項修正（含 tech-lead-mode Path B 對齊解鎖、gemini-assist 移除停服 gemini CLI）|
| `959adf2` v5.0.1 | agy 模式 C 異質對抗審查（8 條：6 修 / 1 駁回 / 1 已知取捨）：三索引鐵律加「專案規則優先、security 底線不得放寬」、`<專案>` 定義 + 無鎖併發合併規約、§7 補 16KB 檢查、lessons 精簡觸發時機 |
| `ff54705` v5.0.2 | Claude 全域載入點補齊：CLAUDE.global.md + setup.sh 第五鏈 `~/.claude/CLAUDE.md` |
| `c26f527` docs | README 對齊 v5.0（架構圖、五 symlink 表、版本表）+ handoff skill 補跨 harness 交接規約（併發防護、回寫自檢）|

## 關鍵架構（接手必知）

```
~/Agent_skill（唯一正本，git 版控）
├── CLAUDE.md（repo 內索引）/ CLAUDE.global.md → ~/.claude/CLAUDE.md
├── AGENTS.md → ~/.codex/AGENTS.md（Codex 全域）
├── GEMINI.md → ~/.gemini/GEMINI.md（agy 全域）
├── governance/ → ~/.claude/governance（制度層七檔）
└── skills/ → ~/.claude/skills
```
- 五條 symlink 全由 setup.sh 建，本機 2026-07-07 已跑並驗證
- 改索引檔必跑 maintenance-protocol §7 防漂移四查（AGENTS/GEMINI 的四個共用段設計為逐字相同）
- 記憶正本：交接 = `~/.agent-sessions/<專案>/latest.md`、教訓 = lessons.md、決策 = ADR；Codex Memories / agy brain 不作數

## 待處理

- [ ] **`git push`**：領先 origin 5 commits，待使用者指示
- [ ] **其他機器重跑 `bash ~/Agent_skill/setup.sh`**（五鏈；不跑則該機 Codex/agy 靜默無制度）
- [ ] 第一個 Codex session：校準 model-orchestration §5 Codex 欄（gpt-5.5/5.4/5.4-mini + effort，官方文件查證但**零本機實測**）
- [ ] 第一個 agy 委派：驗證 `TypeName`/`Workspace` subagent 參數名（CLI 層 `-p`/`--model`/`--print-timeout` 本輪已實測 ✅，session 內委派參數仍未驗）
- [ ] governance/backups/ 已 17 個（門檻 10，使用者本輪選全留）→ 下次 🟡 修改時再問清理
- [ ] `memory/project-context.md` 技術選型欄位仍是模板占位（長期）

## 已知問題 / 風險

- ⚠️ **agy 已可寫檔**：excludeTools 解鎖是使用者決定；若日後 agy 誤寫檔案，這是根因候選，備份在 `~/.gemini/settings.json.2026-07-07.bak` 可一鍵還原
- ⚠️ **未確認事實**（都已在制度檔標註）：Codex 對話中逐次指定 model、agy credit 換算、agy model picker 掛羊頭傳聞、「導向 Opus 4.8 的請求是否耗原方案額度」
- ⚠️ inline 段與 rules/ 正本是人工摘要關係，§7 只保證兩索引互不漂移（已知取捨，見 §7「已知極限」）

## 接手後第一步

1. `git -C ~/Agent_skill status` + `git log origin/main..HEAD --oneline`（預期：乾淨 + 領先 5，除非已 push）
2. 讀 `governance/letter-to-future-sessions.md`（含信心最低清單）
3. 若在新機器：先 `bash ~/Agent_skill/setup.sh`

## 有用的指令

```bash
# 五鏈健檢
ls -l ~/.claude/skills ~/.claude/governance ~/.claude/CLAUDE.md ~/.codex/AGENTS.md ~/.gemini/GEMINI.md

# 索引防漂移四查（改索引後必跑）
# 見 governance/maintenance-protocol.md §7（指令可直接複製執行）

# agy 對抗審查（模式 C，本輪實測可用的呼叫法）
agy --print-timeout 9m -p "<審查指令>" < <打包檔>
```

## 相關連結

- ADR-009：`memory/project-context.md`
- 制度修改權限：`governance/maintenance-protocol.md`（三索引 = 🟡 先問；改後跑 §7）
- 上輪交接：`memory/handoff-20260706.md`（v4.9）
